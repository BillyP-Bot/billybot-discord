version: "3.7"
services:
  bt-bot-dev-container:
    container_name: bt-bot-dev-container
    image: postgres
    restart: unless-stopped
    volumes:
      - ./data/db:/var/lib/postgresql/data
    env_file:
        - database.env
    ports:
      - "6666:5432"
    networks:
      - bt-bot-dev-network
networks:
  bt-bot-dev-network:
    driver: bridge